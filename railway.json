{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx prisma generate && npx prisma migrate reset --force && npx prisma db seed && npx next build"
  },
  "deploy": {
    "startCommand": "npx next start",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
